enum HospitalTypeFilter {
  allType,
  pediatrics,
  moonlight,
}

enum HospitalStatusFilter {
  allStatus,
  openNow,
  openToday,
  openSunday,
}

enum HospitalFilterLocation {
  left,
  right,
}

enum HospitalOperatingStatus {
  unknown,
  open,
  finished,
  notOpenedToday,
}

enum SurveyQuestionType {
  unknown,
  selection,
  text,
}
